name: 发布完整性检查

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    outputs:
      version: ${{ steps.metadata.outputs.version }}
      prerelease: ${{ steps.metadata.outputs.prerelease }}
    runs-on: ubuntu-latest
    steps:
      - name: 检出仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 解析发布版本与索引元数据
        id: metadata
        env:
          EVENT_NAME: ${{ github.event_name }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json, os, re
          index = json.load(open("versions/index.json", encoding="utf-8"))
          version = os.environ["REF_NAME"] if os.environ["EVENT_NAME"] == "push" and os.environ["REF_NAME"].startswith("v") else index["latest_version"]
          if not any(item.get("id") == version for item in index["versions"]):
              raise SystemExit(f"版本索引缺少目标版本：{version}")
          print(f"version={version}")
          print(f"prerelease={'true' if re.search(r'-(?:beta|Beta)[0-9]*$', version) else 'false'}")
          PY
      - name: 构建并校验目标版本
        env:
          VERSION: ${{ steps.metadata.outputs.version }}
          IS_TAG: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v') }}
        run: |
          ./scripts/verify-release.sh
          if [ "$IS_TAG" = "true" ]; then
            if [ -d "versions/$VERSION" ]; then ./scripts/build-release.sh "$VERSION" existing; else ./scripts/build-release.sh "$VERSION" snapshot; fi
          elif [ -d "versions/$VERSION" ]; then
            ./scripts/build-release.sh "$VERSION" existing
          else
            ./scripts/build-release.sh "$VERSION" snapshot
            git diff --exit-code -- "versions/$VERSION"
          fi
          ./scripts/verify-release.sh "$VERSION"
      - name: 保存构建产物
        uses: actions/upload-artifact@v4
        with:
          name: ThemeMusic-${{ steps.metadata.outputs.version }}
          path: dist/${{ steps.metadata.outputs.version }}/
          if-no-files-found: error

  release:
    if: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v') }}
    needs: verify
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 检出仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: 下载已验证产物
        uses: actions/download-artifact@v4
        with:
          name: ThemeMusic-${{ needs.verify.outputs.version }}
          path: release-assets
      - name: 创建或更新 GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ needs.verify.outputs.version }}
          PRERELEASE: ${{ needs.verify.outputs.prerelease }}
        run: |
          awk -v version="$VERSION" '$0 ~ "^## " version "([[:space:]]|$)" {capture=1; next} capture && /^## / {exit} capture {print}' CHANGELOG.md > release-notes.md
          assets=()
          while IFS= read -r -d '' asset; do assets+=("$asset"); done < <(find release-assets -maxdepth 1 -type f -print0)
          test "${#assets[@]}" -gt 0
          if gh release view "$VERSION" >/dev/null 2>&1; then
            existing=$(gh release view "$VERSION" --json assets --jq '.assets[].name')
            missing=()
            for asset in "${assets[@]}"; do
              name=${asset##*/}
              if ! printf '%s\\n' "$existing" | grep -Fxq "$name"; then missing+=("$asset"); fi
            done
            if [ "${#missing[@]}" -gt 0 ]; then
              gh release upload "$VERSION" "${missing[@]}"
            else
              echo "Release $VERSION 资产已完整。"
            fi
            exit 0
          fi
          flags=(--target "$GITHUB_SHA" --title "$VERSION" --notes-file release-notes.md)
          if [ "$PRERELEASE" = "true" ]; then flags+=(--prerelease); else flags+=(--latest); fi
          gh release create "$VERSION" "${assets[@]}" "${flags[@]}"
