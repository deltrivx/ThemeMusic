## v1.3.7 — 2026-08-05

### 修复

- 修复 GitHub 版本索引在镜像缓存滞后时误报“未知版本”的问题。
- 显式安装版本会通过版本清单二次确认后继续安装。

## v1.3.6 — 2026-08-05

### 修复

- 修复安装器校验版本清单后未保存清单内容，导致后续必需文件检查失败的问题。

## v1.3.5 — 2026-08-05

### 修复

- 修复 Release 清单返回非空错误页时未触发版本快照回退的问题。
- 安装器现在只接受通过完整清单校验的下载结果。

## v1.3.4 — 2026-08-05

### 修复

- 修复 Release 资产连接超时后空文件仍进入清单校验，导致更新安装失败的问题。
- 安装器在 Release 资产不可达时回退到版本快照，并对下载文件执行非空校验。

## v1.3.3 — 2026-08-05

### 修复

- 修复手机端独立配置仍复用电脑端播放偏好和续播状态的问题。
- 修复音源循环会切换到未配置 Navidrome 或 FnOS 的问题。
- 优化安装任务失败反馈，显示后台日志尾部和实际退出原因。

### 改进

- 正常曲库提示改为动态循环；配置、网络、解码和空曲库异常持续保留。
- 曲库缓存按设备 profile 和音源范围隔离，切换音源时清理旧播放状态。

## v1.3.2 — 2026-08-03

### 修复

- 修复 GitHub 更新日志 action 响应较慢时页面长时间停留在加载状态的问题。
- 增加远程版本索引和 CHANGELOG 的有限超时、镜像回退及短期缓存，减少网络异常对设置页的影响。
- GitHub 暂时不可达时保留可用的版本列表，并向用户显示明确错误，不再返回无提示的空白结果。

### 兼容性

- 保留 v1.3.1 的 FnOS 认证恢复、歌词非 JSON 处理、无歌词空态和音源切换竞态修复。

## v1.3.1 — 2026-08-03

### 修复

- 修复 FnOS 音源偶发 HTTP 401，导致曲库、歌词、封面或音频请求失败的问题。
- 增加按 FnOS 地址和用户名隔离的短期 token 缓存，并在 token 失效时自动重新登录、重试一次。
- 修复 FnOS 登录失败后继续发送未认证请求的问题。
- 修复歌词响应为 HTML、登录页、空响应或非 JSON 时前端解析失败的问题。
- 修复 Navidrome 和 FnOS 没有歌词时错误返回 HTTP 502 的问题，统一返回正常的空歌词状态。
- 修复切换音源后曲库加载与再次切换之间的异步竞态。
- 修复移动端 FnOS 配置项宽度不足，以及桌面端配置控件延迟后宽度缩短的问题。

### 改进

- 统一 FnOS 曲库、歌词、封面和音频代理的认证处理。
- 改进歌词错误提示，区分认证、网络、HTTP 和响应格式错误。
- 优化音源切换后的播放器状态清理和曲库加载流程。
- token 缓存文件权限限制为 `0600`，不改变现有配置和密钥。

## v1.3.0 — 2026-08-02

### 正式版重点更新

- 新增飞牛音乐 HTTP 音源，支持曲库、封面、时长、真实播放和歌词。
- 统一本地、Navidrome 和飞牛音乐三种音源的歌曲信息、播放控制和状态显示。
- 完善歌词读取，支持 Navidrome 和飞牛音乐远端歌词，以及本地同目录 `.lrc` / `.txt` 文件。
- 优化大曲库加载，支持后台索引、缓存恢复、分段显示和曲目总数提示。
- 新增歌词时间校准，支持实时调整并保存不同音源的偏移。
- 支持在音乐卡片底部循环切换音源，切换时不刷新整个 Dashboard。
- 修复页面刷新后的播放状态抖动和播放恢复问题。
- 修复音源配置保存和设置页布局问题。


## v1.3.0-beta9 — 2026-08-02

### 修复

- **3 个音源设置项独立配置 + 显隐隔离**：此前 `ThemeMusic.page` 接收 POST 时把 `MUSIC_LOCAL_DIR` / `MUSIC_NAVIDROME_URL` / `MUSIC_FNOS_URL` 等 3 个音源的字段**全部写回 cfg**，导致切换音源时其它音源的旧值被重新存盘（最严重：切到 Navidrome 后本地音乐目录被空字符串覆盖）。本次修复：
  - PHP 仅在 `MUSIC_SOURCE` 匹配当前选中音源时**才**收对应字段的 POST，未选中音源的字段保持 cfg 旧值不动
  - `MUSIC_LOCAL_DIR` 仅当 `MUSIC_SOURCE === "local"` 时写入
  - `MUSIC_NAVIDROME_URL / USER / PASSWORD` 仅当 `MUSIC_SOURCE === "navidrome"` 时写入
  - `MUSIC_FNOS_URL / USER / PASSWORD` 仅当 `MUSIC_SOURCE === "fnos"` 时写入
- **音源切换时显隐由 JS `syncSourceRows` 实时刷新**：本地/Navidrome/飞牛的输入框与「测试连接」按钮按当前音源单独显示/隐藏（`hideRow` 走 dl/tr/字段自身回溯，dt+dd 同步隐藏，邻接 blockquote 提示也跟随隐藏），不再全列展示。切换瞬间生效，保存不丢隐藏音源的值（POST 不被表单隐藏，但 PHP 端按 SOURCE 过滤后只写当前音源）

## v1.3.0-beta8 — 2026-08-02


### 修复

- **飞牛音乐测试连接不再显示 unknown**：后端 `ucwc-music-api.php` 此前没有 `fnos_test` action，前端点击统一落到兜底分支返回 `unknown action`；本次新增 `fnos_test` action（参照 `navidrome_test` 模板），调用飞牛 HTTP API 的 `track/list` 探活，返回 `server_type` / `library_access`；同时支持表单内 `url` / `user` / `password` 临时覆盖配置
- **存储准备按策略分流显示，去除混显示**：前端 `wireStorageStatus` 新增 `detectedLabel()` 工具函数，`自动检测` 标签按 `strategy` 单独渲染
  - 飞牛/Navidrome → 显示音源类型
  - 本地磁盘 → 显示「本地磁盘等待唤醒」（不再和 SMB 串在一起）
  - Unraid 用户共享 → 显示对应提示
  - SMB → 显示「SMB 远端默认挂载」
  - NFS → 显示「NFS 远端默认挂载」
  - 无效 → 显示「本地音乐目录不可访问」
- **飞牛音乐三框等宽到侧边**：`assets/theme-music-settings.css` 把 `#tm-fnos-url / user / password` 加入 Navidrome 那条 `var(--tm-ctrl-w, 14rem)` 的等宽选择器组；`max-width: 100%` + `box-sizing: border-box` 防止溢出右侧
- **取消设置页底部「清理启动盘旧缓存」按钮**：HTML 段 (`#tm-cache-actions`)、JS 端 `wireCacheClear()` 函数、其 `formatBytes()` 工具函数、`bootUi()` 引用一并删除；后端 `clear_cache` action 保留（不影响运维能力）
- **取消「卡片显示规则」说明段**：删除 ThemeMusic.page 中「仪表盘不再提供单独的关闭卡片按钮」独立行
- **取消「附属文件」说明段**：与清理缓存按钮同段一并删除，去除冗余提示

## v1.3.0-beta7 — 2026-08-02

### 修复

- **设置页布局错乱**：比对 v1.2.2 正式版结构逐行修复飞牛音乐配置区块
  - Navidrome 连接行的 `<span class="tm-inline-actions">` 此前未闭合，导致后续所有行被吞进该 span
  - 飞牛「地址 / 用户 / 密码 / 连接」四行的定义列表标签存在多余缩进，破坏 Unraid `dl/dt/dd` 渲染
  - 飞牛连接行结尾误用 `</div>`，应为 `</span>`；该多余闭合标签破坏整页 DOM 层级
  - 文件末尾丢失换行已补回
- **飞牛测试连接按钮无响应**：`wireFnosTest()` 此前只定义未调用，现已在初始化流程中接上
- 飞牛输入框属性对齐 Navidrome：地址 `maxlength=512` / `autocomplete=url`，用户 `autocomplete=username`
- **还原被污染的 v1.2.2 正式版归档**：`versions/v1.2.2/` 下的 `ThemeMusic.page`、`ucwc-music-api.php`、`files.manifest` 曾被 beta 内容覆盖，现恢复为 v1.2.2 发布时的原始内容

## v1.3.0-beta6 — 2026-08-02

### 修复

- **重写飞牛音乐(FnOS)音源**：从 SSH+sqlite3 方式彻底改为 HTTP API 方式
- 移除 SSH 连接和 sqlite3 命令依赖，改为通过 HTTP API 获取曲库、流媒体、封面、歌词
- 配置项改为「飞牛音乐地址 + 用户名 + 密码」，与 Navidrome 配置一致

### 配置项变化

| 旧配置（已移除） | 新配置 |
|---|---|
| `MUSIC_FNOS_HOST` | `MUSIC_FNOS_URL`（飞牛音乐 HTTP 地址） |
| `MUSIC_FNOS_DB` | `MUSIC_FNOS_USER`（飞牛音乐用户名） |
| `MUSIC_FNOS_MUSIC_DIR` | `MUSIC_FNOS_PASSWORD`（飞牛音乐密码） |

### 文件改动

| 文件 | 改动 |
|------|------|
| `ucwc-music-api.php` | 删除 SSH+sqlite3 代码，新增 HTTP API 函数 |
| `ThemeMusic.page` | 设置页改为「地址 + 用户名 + 密码」输入框 |

## v1.3.0-beta — 2026-08-02

### 新增

- **飞牛音乐(FnOS)音源**：新增 `fnos` 音源类型，通过 SSH 直连 FnOS 宿主机读取飞牛音乐的 SQLite 数据库，获取曲库、元数据与文件路径，无需额外服务端 API
- 支持飞牛音乐已扫描的曲目、专辑、艺术家、时长、码率、采样率等完整元数据
- 封面与歌词沿用 ThemeMusic 本地文件匹配机制，从歌曲同目录 `.lrc` / `.jpg` 文件读取

### 配置

| 设置项 | 说明 |
|--------|------|
| 宿主机地址 | FnOS 宿主机 IP，默认 `192.168.31.5`，需已配置 SSH 免密 |
| 数据库路径 | 飞牛音乐 SQLite 数据库路径，默认 `/usr/local/apps/@appdata/trim.music/db/music.db` |
| 音乐目录 | 曲库根目录，默认 `/vol2/1000/Music` |

### 文件改动

| 文件 | 改动 |
|------|------|
| `ucwc-music-api.php` | 新增 `fnos` 音源类型，含扫描 worker、list、cover、lyric、stream 路由 |
| `ThemeMusic.page` | 设置页新增飞牛音乐配置项（宿主机/数据库/音乐目录） |
| `versions/index.json` | 新增 v1.3.0-beta |
| `theme.music.plg` | 版本号更新至 v1.3.0-beta |
| `README.md` | 当前正式版与安装命令更新至 v1.3.0-beta |

## v1.2.2 — 2026-08-01

### 修复

- **胶囊与卡片的上一曲/下一曲恢复可用**：v1.2.1 移除音频宿主弹窗时，`next()` 与 `prev()` 中全站播放分支的宿主消息调用被删成空块并直接 `return`，导致点击无任何反应
- **胶囊与卡片的播放/暂停恢复可用**：`togglePlay()` 中的全站播放分支同样被删成空块，播放暂停键一并失效
- 三个控件现在与仪表盘卡片走同一条内联 `<audio>` 控制路径，符合无弹窗架构

### 文件改动

| 文件 | 改动 |
|------|------|
| `assets/ucwc-music.js` | 移除 `next()` / `prev()` / `togglePlay()` 中三处只剩 `return` 的全站播放空壳分支 |
| `versions/index.json` | 新增 v1.2.2，降级 v1.2.1 |
| `theme.music.plg` | 版本号更新至 v1.2.2 |

## v1.2.1 — 2026-08-01

### 变更

- **移除音频宿主弹窗**：全站播放不再弹出 380×112 独立音频宿主窗口（`window.open`）
- **音乐胶囊改用内联音频**：胶囊（chip）模式使用与仪表盘卡片相同的内联 `<audio>` 元素，不再依赖宿主弹窗
- **跨页续播移除**：页面导航后音频停止，不再跨页面保持播放（这是无弹窗的必然代价）
- 所有 v1.2.0 改进保留：大曲库后台扫描、歌词时间校准、300 首分段渲染、曲库缓存 v3

### 删除的文件

- `assets/ucwc-music-host.html`：不再需要独立音频宿主弹出页

### 文件改动

| 文件 | 改动 |
|------|------|
| `assets/ucwc-music.js` | 删除全部宿主弹窗系统（常量/变量/函数约 15 个），胶囊改用内联 `<audio>` |
| `assets/ucwc-music-host.html` | **删除** |
| `assets/ucwc-music.css` | 胶囊内联音频样式微调 |
| `ThemeMusic_Loader.page` | 移除宿主相关注入 |
| `versions/index.json` | 新增 v1.2.1，降级 v1.2.0 |
| `theme.music.plg` | 版本号更新至 v1.2.1 |

# Theme Music Changelog

## v1.2.0 — 2026-08-01

### 大曲库完整索引与低负载刷新
- 保留 v1.1.4“一目录只读取一次、同目录批量识别歌词/封面、禁止跟随软链接”的防重复扫描设计
- 移除本地曲库在 Web 请求中的 1200 首、6 层目录和 3 秒同步硬截断，改由 `nice(10)` 低优先级 CLI 后台任务完成索引
- 相同音源使用文件锁保证同一时刻只有一个扫描任务；重复刷新只读取状态，不再叠加 CPU 与存储 I/O
- 扫描期间继续提供上一次完整索引，完成后通过临时文件与原子重命名一次切换，避免半写入列表
- 曲目列表每次只渲染 300 首并可继续加载，服务器与播放宿主保留完整曲库；超大索引不再强塞浏览器会话缓存

### 歌词时间校准
- 在重新匹配歌词按钮旁增加“提前 0.5 秒”“延后 0.5 秒”和当前偏移值三个控制
- 校准值按音源与曲目保存，刷新、切页、重新匹配歌词后保持；只有点击中间偏移值才手动重置为 `0.0s`
- 偏移限制为正负 10 秒，保留 LRC 文件自带 `[offset:]`，不改写原始歌词文件

### 设置与正式发布同步
- “清理启动盘旧缓存”按钮改为独占一行并水平居中，说明文字与操作状态分层显示
- PLG、README、CHANGELOG、版本索引、`versions/v1.2.0` 快照和文件清单统一同步
- GitHub Actions 在完整性检查通过后自动创建 `v1.2.0` 正式 Release 并上传归档、PLG、安装器、清单及校验和

## v1.1.9 — 2026-08-01

### 存储准备自动分流
- 播放前按实际文件系统自动识别 Unraid 本地阵列盘、用户共享、SMB/CIFS、NFS 与普通本地文件系统
- 只有本地阵列盘调用 `emcmd` 唤醒；SMB/NFS 仅通过轻量读取等待远端存储就绪，两个功能与状态提示完全分开
- 设置页显示当前检测方案与最近一次播放结果，例如“disk3 已唤醒”或“SMB 远端存储已就绪”

### 附属文件退出启动盘
- 本地与 SMB/NFS 音源下载的歌词固定原子写入歌曲同目录 `歌曲名.lrc`
- 下载或提取的封面统一转换并原子写入歌曲同目录 `歌曲名.jpg`，播放前优先复用现有附属文件，避免重复下载
- 歌曲目录不可写时明确返回错误，不再静默回退到 `/boot`；Navidrome 继续使用服务端 API 和浏览器缓存
- 安装或升级时只删除 `/boot/config/plugins/theme.music/lyrics-cache` 与 `cover-cache` 两个旧动态缓存目录，保留配置、密码和插件状态

## v1.1.8 — 2026-08-01

### 单一宿主持久播放
- 全站胶囊/卡片+胶囊模式改由一个固定名称、同源的音频宿主持有 `Audio`，Tower 页面刷新和内部切页只重新连接，不再重建播放进程
- Tower 页面内的音乐胶囊外观与交互保持不变，曲目、播放状态、进度、音量、随机和循环状态通过同源消息双向同步
- 切换到非 Tower 页面后自动暂停，重新回到 Tower 时仅恢复系统自动暂停的播放；用户手动暂停不会被自动恢复
- Tower 页面关闭后先停止播放，再在客户端心跳过期后关闭宿主；短暂导航宽限避免把正常刷新误判为离开

### 单窗口安全约束
- 宿主只能在用户明确点击播放、上一首、下一首或曲目时创建，自动播放、心跳、计时器和页面生命周期事件均不得调用开窗
- 使用固定窗口名和跨标签短时启动锁，即使连续点击或多个 Tower 标签同时操作也只复用一个宿主
- 发布检查新增宿主 JavaScript 语法、运行文件清单、唯一开窗点、固定窗口名和“宿主不得再开窗”断言

## v1.1.7 — 2026-07-31

### 许可与安装默认值
- 程序代码由 MIT 改为 PolyForm Noncommercial License 1.0.0，个人及非商业组织可在非商业目的下使用、修改和分发，商业使用须取得书面授权
- 首次安装写入 `SERVICE="disabled"`，用户需在设置页手动开启；升级继续保留已有服务开关
- 设置页与 Loader 在服务配置缺失时均采用关闭作为安全默认值

### 更新日志布局
- 版本列表移除横向滚动，长版本说明和标签可在左栏内自动换行
- 双栏网格允许内容收缩，手机窄屏自动切换为单列，弹窗和日志正文只保留纵向滚动

## v1.1.6 — 2026-07-31

### 胶囊跨页播放保活
- 全站胶囊或卡片+胶囊模式不再因 `visibilitychange`、`pagehide`、`beforeunload` 或页面冻结而主动暂停当前音频
- 页面切换时只持久化曲目、进度和播放意图；Unraid 软导航与卡片重绘继续复用正在播放的 Audio 进程
- 卡片独立模式仍在页面离开时暂停，避免离开仪表盘后后台误播

### 循环模式辨识
- 关闭循环显示带斜杠的循环图标，列表循环显示标准循环箭头，单曲循环显示带数字 1 的图标
- 三种状态分别使用独立状态类、无障碍标题和图标标记，连续切换与 Unraid 重绘后保持一致

## v1.1.5 — 2026-07-31

### 跨页播放状态稳定
- 将切页、刷新、`pageshow`、可见性恢复和媒体事件触发的续播请求收敛为单一重试链，避免并发 `play()` Promise 竞争
- 页面隐藏、用户明确暂停、音频重建时立即作废旧播放回调，旧音频的延迟事件不再覆盖当前状态
- 卡死检测只处理已进入播放但进度不前的音频，不再把被自动播放策略拦截的暂停状态误判为卡死
- 遇到 `NotAllowedError` 时仅完成普通与静音兼容尝试，随后稳定等待用户点击播放，不再周期性销毁和重建音频

### 右侧复用区高度
- 右侧标题栏固定为 35px，避免信息模式隐藏工具键后标题栏收缩
- 歌曲信息模式同步固定内容高度和 flex 基准，与“搜索栏 36px + 曲目区”保持相同总高度
- 桌面与 390px 手机布局回归均确认曲目、歌曲信息切换前后卡片及右侧区域像素高度完全一致

## v1.1.4 — 2026-07-31

### 大曲库安全扫描
- 本地曲库改为广度优先的有界扫描：最多 1200 首、6 层目录、1200 个目录、24000 个条目和 3 秒墙钟时间
- 不再跟随符号链接，跳过回收站和常见系统目录，避免循环目录与超大共享拖死 emhttp
- 每个目录只读取一次，同时建立歌词和封面索引，移除逐曲目重复扫描目录造成的近似二次方 I/O
- 服务端曲库缓存从 2 分钟延长到 6 小时，手动“重新加载曲库”仍可明确强制刷新
- 浏览器会话保存最多 1200 首曲库，刷新和切页直接恢复曲目及播放状态，不再先触发全局扫描

### 播放器交互与歌曲信息
- 随机、上一首、播放/暂停、下一首、循环五键在 `pointerdown` 阶段立即执行，并抑制随后合成的重复点击
- 通用媒体解锁逻辑不再抢占卡片和胶囊控件的用户手势，修复需要连续点击多次才生效
- 歌曲信息不再使用独立弹窗，改为复用右侧曲目/歌词区域；再次点击信息键可返回原区域
- 从歌曲信息状态点击曲目/歌词切换键会直接切换目标区域，Unraid 克隆重绘后仍保留完整响应

### Unraid 插件列表
- 对齐 Theme Effects 的安装登记方式，终端/OTA 安装将 `/var/log/plugins/theme.music.plg` 注册为指向闪存 PLG 的软链接
- PLG 管理器内安装时清理错误的普通文件，由 Unraid `plugin` 流程在安装结束后建立标准注册链接

## v1.1.3 — 2026-07-31

### 安装链路稳定性
- 优先从 GitHub Release 一次下载约 150 KB 的版本归档，避免逐个访问 Raw 文件造成长时间阻塞
- 使用 `SHA256SUMS` 验证归档，再按 `files.manifest` 对每个运行文件二次校验
- OTA 继续复用本地未变化文件，只写入发生变化的运行项
- Raw 与镜像回退的单端点等待从最长 300 秒×3 次收紧为 30 秒，故障切换更及时
- Release 下载增加备用加速入口；旧版本或归档不可用时仍自动回退到逐文件安装
- macOS 构建时禁用扩展属性打包，消除 Linux/Unraid 下的 `._*` AppleDouble 条目与解包警告

### 实机验证
- 在 Unraid 7.3.2 复现并定位 Raw 连接已建立但无数据的阻塞场景
- 中断测试安装前未发生运行文件写入，现有版本状态保持完整

## v1.1.2 — 2026-07-31

### 仓库与项目说明
- README、About、插件管理器说明和重要版本沿革统一为正式中文内容
- 增加贡献、安全、支持、问题模板与拉取请求模板，形成完整独立项目结构
- 明确双许可证边界：程序代码采用 MIT，文档与品牌素材采用 CC BY-NC-SA 4.0

### 安装与发布
- Release 标准产物增加独立 `files.manifest` 和 `install.sh`，并由 `SHA256SUMS` 统一校验
- OTA 安装成功后同步当前版本 PLG 到闪存和插件日志目录，解决运行时与插件列表版本不一致
- 插件管理器中文说明纳入版本包、持久化目录与闪存离线恢复

### 质量保障
- 增加自动化发布验证，覆盖 JSON、脚本语法、PLG XML/内嵌脚本、文件清单、哈希和文档完整性
- 修正历史版本构建只能读取对应快照，避免缺失文件被当前源码静默替代
- 建立统一的 Release 产物结构和仓库级维护说明

## v1.1.1 — 2026-07-31

### 卡片显示规则
- 移除 Unraid 磁贴管理动态注入的“关闭卡片”功能键，标题栏只保留设置与折叠
- 卡片与胶囊是否显示仅由 Theme Music 总开关及电脑/手机运行模式决定
- 清除旧的临时 `collapsed` 隐藏状态，避免显示状态脱离设置控制

### 重绘与设置完善
- 关闭键通过 CSS 即时隐藏，并由 DOM 观察器清除后续动态注入或克隆残留节点
- 保留原生折叠箭头及重绘后的内容、无障碍状态同步
- 设置页增加明确的卡片显示规则说明

## v1.1.0 — 2026-07-31

### Navidrome 正式支持
- 支持 Navidrome/OpenSubsonic 曲库、搜索、排序、封面、Range 串流及结构化/普通歌词
- 设置页可使用当前填写的地址、用户名和密码直接测试，密码继续独立以 `0600` 权限保存
- 保留本地目录音源，并支持 FnOS 音乐挂载恢复后自动重启 Navidrome

### 卡片完整交互
- 卡片与全局音乐胶囊的上一首、播放/暂停、下一首保持即时双向同步
- 随机、循环、曲目/歌词切换、排序、刷新、歌词重匹配、曲目选择和信息弹窗统一为稳定事件入口
- 搜索、音量和播放进度不再依赖会被 Unraid 克隆丢失的节点监听器
- 原生折叠箭头在仪表盘重绘后立即同步卡片内容和无障碍状态

### 稳定性与验证
- Unraid 重绘/克隆卡片后，全部卡片控件无需等待定时器即可恢复响应
- 搜索采用 60ms 合并刷新，兼顾即时反馈与大曲库低负载
- 完整 DOM 事件回归中按钮响应为 0–17ms；Unraid 实测 Navidrome 38 首曲库接口约 25ms

## v1.1.0-beta3 — 2026-07-31

### 卡片与胶囊双向同步
- 音乐卡片与全局音乐胶囊的上一首、播放/暂停、下一首统一使用同一控制链路
- 切歌后立即同步两处曲名与按钮状态，不再等待远程音频 `loadedmetadata` 或 `canplay`
- 音频真实的 `play` / `pause` 事件仍是最终状态来源，播放失败时不会保留错误的播放状态
- Unraid 重绘或克隆音乐卡片后，文档级事件委托仍能接管三个功能键

### 回归验证
- 覆盖卡片到胶囊、胶囊到卡片、卡片替换及两端交叉快速点击
- DOM 事件回归中单次界面响应为 2–17ms，全部低于 50ms 目标

## v1.1.0-beta2 — 2026-07-31

### 播放与卡片
- “上一首”每次点击都切换到前一曲，不再在播放超过三秒时只重播当前曲目
- 动态磁贴关闭键使用绝对居中的字形，与设置、折叠键统一为 30×30
- 卡片保留“音乐 / 音乐播放器”标题，并补充与 Unraid 其他卡片一致的 1px 标题分割线

### Navidrome
- 用户名与密码的初始默认值保持为空
- “测试连接”直接使用输入框当前内容，不必先应用保存；密码留空时仍可测试已保存密码
- 实测 Navidrome 0.63.2 结构化歌词时间轴正常；无歌词曲目明确保持空状态，不误报为解析失败

### FnOS 挂载
- `mount_fnos_smb` 增加 `/mnt/remotes/FnOS/Music` 就绪检查，并在挂载恢复后重启 Navidrome

## v1.1.0-beta — 2026-07-31

### Navidrome 音源
- 设置页增加 Navidrome 地址、用户名、独立密码和连接测试
- 使用 Subsonic 1.16.1 加盐令牌鉴权，密码仅写入 `navidrome.secret`（权限 `0600`）
- 支持 Navidrome 曲库分页读取、两分钟缓存、搜索、排序和手动刷新
- 音频经插件同源代理转发，完整保留 HTTP Range，浏览器不接触 Navidrome 密码
- 接入 Navidrome 封面、同步歌词和无时间轴歌词
- 保留本地目录音源，可在两种音源之间切换；升级不会覆盖现有配置

## v1.0.19 — 2026-07-31

### 自动续播状态
- 浏览器确认音频已经开始播放后，立即清除“正在尝试自动续播”等临时提示
- 正常播放期间恢复显示曲库数量，避免成功状态仍被误报为续播中

## v1.0.18 — 2026-07-31

### 原生磁贴控件
- 动态磁贴关闭键与折叠箭头固定使用白色图标，避免深色主题变量返回黑色
- 不再使用 `display: inline-flex !important` 覆盖 Dynamix 的 `i.tile` 状态
- 关闭键平时隐藏，仅在 Unraid“磁贴管理”模式动态显示；显示后继续保持 30×30 对齐

### 音乐磁盘自动唤醒
- 每次新音轨流首次读取前执行存储预唤醒
- `/mnt/diskN` 与 Unassigned Devices 使用 Unraid `emcmd cmdSpinup` 请求唤醒
- `/mnt/user`、CIFS 与 NFS 音源执行一字节轻量读取，等待本地或远端磁盘就绪后再响应媒体流
- 同一曲目设有 20 秒唤醒冷却，避免浏览器 Range 并发请求重复唤醒

### 低配设备负载优化
- 歌词同步由每帧 `requestAnimationFrame`（通常 60 次/秒）改为每 250ms 更新一次
- 歌词未切换时不再重复刷新全局音乐胶囊 DOM
- 曲库递归扫描增加 120 秒服务端缓存；手动“重新扫描”仍强制绕过缓存
- 当前 Unraid 实测 38 首远程曲库：完整扫描约 171ms，缓存读取约 18ms

## v1.0.17 — 2026-07-31

### 仪表盘标题栏
- 音乐卡片标题图标固定为白色
- 移除自定义“重新扫描”和“本次隐藏”按钮，保留设置与 Unraid 原生功能键
- 接管原生折叠箭头状态，使其真实折叠、展开音乐卡片内容
- 将动态磁贴管理模式注入的关闭键移入标题栏，与设置和折叠键统一为 30×30

### 曲目与歌词复用区
- 曲目模式继续显示搜索框
- 歌词模式隐藏搜索行并将释放出的 36px 分配给歌词区
- 桌面歌词可视区由 168px 增至 204px，移动端由 148px 增至 184px
- 切换曲目与歌词时右侧面板总高度保持不变

### 验证
- PC 与 390px 移动端标题栏控件对齐偏差均为 0
- 原生箭头连续折叠、展开及模拟 Unraid DOM 重绘后均可正常工作
- 随机与循环按钮在重绘后连续点击回归通过

## v1.0.16 — 2026-07-31

### 交互修复
- 随机与循环改用文档级捕获委托，不依赖会被 Unraid 克隆但不会携带监听器的 `data-*` 绑定标记
- 仪表盘替换或克隆卡片后直接接管实时节点，不再追加第二张重复卡片
- 按钮、链接、滑块和曲目项在鼠标、指针与触摸按下阶段阻止冒泡到 sortable，避免拖拽接管点击
- PC、390px 移动端及模拟 Unraid DOM 重绘场景均通过连续点击回归

### 仪表盘外观
- 卡片复用 Unraid 原生 `tbody.dashboard` 背景、边框、标题字号和功能键配色
- 移除独立悬浮阴影、圆角和 hover 上浮效果，桌面与移动端均跟随所在仪表盘列宽
- 标题区采用原生音乐图标、主标题和副标题结构
- 右上角补充**重新扫描曲库**、**打开插件设置**和**隐藏卡片**三个功能键

### 发布规范
- 插件清单、OTA 索引、版本目录、文档、Git 标签与 GitHub Release 统一使用 `v1.0.16`
- GitHub Release 提供 ZIP、tar.gz、插件安装文件及 SHA256 校验清单
- Release 更新日志统一使用中文

## v1.0.15 — 2026-07-31 (`2026.07.31m`)

### 修复
- 状态栏仅保留 **共 N 首**（及截断/筛选），去掉排序文案后缀
- **随机 / 循环** 改为卡片根事件委托绑定，避免 DOM 重绑后二次点击无响应；`updateModeBtns` 始终作用在 live 节点；`saveLs` 延后写入减轻卡顿

## v1.0.14 — 2026-07-31 (`2026.07.31l`)

### 本地列表
- **排序切换**：曲目区工具栏增加排序按钮，循环 歌手 → 曲名 → 路径 → 大小（记忆到 localStorage）
- **徽标**：列表项显示「词」「封」标记（有歌词 / 有封面）；状态栏附带当前排序名

## v1.0.13 — 2026-07-31 (`2026.07.31k`)

### 状态行对齐
- 音量下方 **信息图标 / 音源文案 / 提示** 统一 16px 行高并垂直居中，避免图标相对文案上下偏移

## v1.0.12 — 2026-07-31 (`2026.07.31j`)

### 仪表盘卡片 polish
- **曲目 ⇄ 歌词** 切换时右侧搜索栏始终占位（36px），卡片高度不再跳动
- **重新扫描** / **重新匹配歌词** 使用各自专属图标（文件夹刷新 / 文稿+刷新），避免混淆
- 音量下方状态行改为：`[信息]` + **音源文案**（本地音源，后续可扩展）+ 原有提示文案；点击信息可查看当前曲目详情

## v1.0.11 — 2026-07-31 (`2026.07.31i`)

### 本地曲库体验
- 扫描上限提升至 **2000**（深度 8）；达到上限时 API/状态栏提示**已截断**
- 曲目区增加**搜索**（曲名/歌手/专辑）与**重新扫描**按钮
- 歌词模式可**重新匹配**当前曲（`force=1`，优先网络，失败回退 sidecar）
- 设置页增加**清理歌词/封面/全部缓存**（仅插件下载缓存，不删音乐库文件）

## v1.0.10 — 2026-07-31 (`2026.07.31h`)

### 左右分栏（填满复用区空白）
- **左侧固定**理想宽度（约 286px，仅在列过窄时才收缩）
- **右侧吃满**卡片内剩余宽度：去掉 280px 硬顶，歌词/曲目复用区不再右侧留空条
- 卡片仍 `width:100%` 铺满列，不撑开其它仪表盘卡片

## v1.0.9 — 2026-07-31 (`2026.07.31g`)

### 仪表盘宽度 / 落位（对齐系统卡片）
- 卡片改为 **`width: 100%` 铺满所在列**（与 Docker/系统信息等 tile 一致），随 **两栏 / 三栏** 列宽自适应
- 宿主改为 Unraid 原生 **`tbody.sortable` 挂入 `#db_box1/2/3`**（可跨列拖动、cookie 排序兼容）
- 内部左右分栏由 `fitDashColumnLayout()` 按**实时列宽**分配；右侧仍硬顶 **280px**；列过窄时自动上下堆叠，不撑开其它卡片

## v1.0.8 — 2026-07-31 (`2026.07.31f`)

### 右侧曲目/歌词区
- `fitRightPane` 改用**列表项实际字体**测量（修浏览器/CJK 字宽偏差）
- 列宽上限只认真正的仪表盘列（table-cell 等），避免 `max-content` 祖先把宽度自我锁死在偏窄值
- 仍硬顶 **280px**，不撑开其它卡片

## v1.0.7 — 2026-07-31 (`2026.07.31e`)

### 右侧曲目/歌词区
- 复用栏宽度按最长曲名**动态测量**，并硬顶 **280px**（原列宽上限），不撑开仪表盘整列/其它卡片
- 短曲名时栏更窄，减少不同浏览器字宽导致的「长短不一」观感

### 仪表盘位置
- 位置记忆增加服务端 `dash-pos.json`（`action=dash_pos`），跨浏览器 / 重启仍可恢复
- 更严拒绝「误插到第一列第一个」的 race 覆盖；服务端位加载后再稳定落位

## v1.0.6 — 2026-07-31 (`2026.07.31d`)

### 空库 / 未配置提示
- 初始无曲目时，「请在 Theme Music 设置中配置…」等错误**只**显示在音量下方的 status 行
- 歌名下方副标题不再复用同一段 `state.error`，避免标题下与音量下重复提醒

## v1.0.5 — 2026-07-31 (`2026.07.31b`)

### 开机恢复（plg）
- 插件脚本**先**从 flash 恢复 emhttp runtime，再尝试 OTA
- 网络/GitHub 超时不再把已装插件打进 `plugins-error` 导致「重启后丢失」
- 有完整 flash 包时离线 boot 仍 `exit 0`；包 id **v1.0.5**（内容同 v1.0.4 业务 + 官方 boot 路径）

## v1.0.4 — 2026-07-30 (`2026.07.30e`)

### 歌词错配修复（深度）
- **根因**：侧车 `.lrc` 仅按文件名 stem 关联，不校验正文；网易云弱匹配曾把「别怕我伤心」写入 `09.回来.lrc` 等多首曲目
- 新增 `m_lrc_matches_track`：校验 `[ti]`、正文横幅（`歌手-歌名`）、粘连歌名
- `m_find_matching_lrc`：错配侧车自动隔离为 `.bad-*`，并在 `fetch=1` 时重新拉取
- lrclib / 网易云：要求强标题相关；**禁止**回退到搜索第一首
- 写入前最终校验；插件 lyrics-cache 同样校验后才使用

## v1.0.3 — 2026-07-30 (`2026.07.30d`)

### 歌词同步
- 对齐 **ThemeEffects beta 内置音乐** 时钟：`Math.floor(currentTime*1000) - offsetMs`
- **去掉固定 +160ms lead**（此前人工提前量导致高亮长期偏离 LRC 时间戳）
- 保留 rAF 跟拍与 `driftMs` 可选微调（默认 0）

## v1.0.2 — 2026-07-30 (`2026.07.30c`)

### 布局
- 加宽左侧控制栏（`left-w` 286px），降低右栏 z-index，避免「曲目/歌词」切换钮被右栏遮挡
- 左侧 `overflow: visible`，按钮行完整露出 6 个控件

### 歌词同步
- `lyricClockMs`：按 A2 正确处理 LRC offset（正值延迟显示 → 减时钟）
- 播放中 rAF 跟拍 + seeked 强制同步；仅当前行高亮/跑马灯
- 固定 lead 校准高亮与人声起点

## v1.0.1 — 2026-07-30 (`2026.07.30a`)

### 布局与歌词
- 卡片左右栏间距加大并隔离层叠，避免右侧歌词/曲目区与左侧重叠
- 歌词**仅当前行**在过长时横向滑动；其它行静态省略，不再整列跑马灯
- 歌词时间轴同步校准（offset + 轻微提前量）；纵向只滚动歌词面板内当前行

### 仪表盘与移动端
- 音乐卡片位置持久化加强（`dash_pos_v3`）：刷新/重装后尽量恢复用户放置位置，默认不再强制左上第一位
- 移动端设置页底部「应用 / 完成」按钮居中，修复左偏

---

## v1.0.0 — 2026-07-30 (`2026.07.30`)

正式版。自 ThemeEffects 拆出的独立 Unraid 音乐插件；汇总 Beta1–Beta9 可用能力。

### 设置
- **音乐音源**置顶；「本地音乐目录」随音源显隐（仿主题特效壁纸 hideRow）
- 音源选项文案：**本地目录**（去掉 V1 后缀）
- **电脑 / 手机**分设：运行模式、默认音量、自动播放、随机、循环
- 手机运行模式默认「和电脑端配置相同」，选此项隐藏其余手机项
- **运行模式**三选项：音乐卡片（仅仪表盘）/ 音乐胶囊（全站）/ 卡片+胶囊（全站）
- 标题后 **SERVICE** 总开关；取消表单「播放范围」
- 设置页控件统一列宽；路径浏览对齐主题特效；独立检查更新 / 更新日志

### 播放与跨页
- 仪表盘音乐卡片 + 全站 mini 胶囊（页内浮层，不新开标签）
- 本地目录曲库、LRC 歌词、封面缓存
- 跨页续播 intent；仪表盘 tile 被替换时卡片/进度重建
- 移动端：划掉标签 / 切后台暂停 HTMLAudio，保留续播 intent
- FLAC/CIFS seek 失败硬重置，避免假播放卡死

### 布局
- 卡片左右固定宽；曲目/歌词区加宽；长标题跑马灯不撑破卡片

### 说明
- 插件 id：`theme.music`；与 ThemeEffects ≥ v2.6.0 并存（主题侧已无内置音乐）

---

## v1.0.0-Beta9 — 2026-07-30 (`2026.07.30i`)

### 设置
- **电脑 / 手机** 分设：运行模式、默认音量、自动播放、随机、循环
- 手机运行模式默认「和电脑端配置相同」；选此项时隐藏其余手机项（仿主题特效 hideRow）
- 音源与本地目录仍共享；标题 `SERVICE` 仍为总开关

### 运行时
- Loader 注入 `mobile` 配置块；客户端按触控/窄屏选用手机配置
- **划掉标签页 / 切后台**：暂停 HTMLAudio，保留续播 intent（修复移动端进程后台仍播）

---

## v1.0.0-Beta8 — 2026-07-30 (`2026.07.30h`)

### 设置
- 「音乐组件」改为 **运行模式**：音乐卡片（仅仪表盘）/ 音乐胶囊（全站）/ 卡片+胶囊（全站）
- 取消「播放范围」；关闭播放器用标题后总开关（`SERVICE`）
- 配置键 `MUSIC_RUN_MODE=card|chip|both`，并派生 `MUSIC_ENABLE` / `MUSIC_DASH_ONLY` / `MUSIC_UI`
- 旧「全站」(`DASH_ONLY=no`) 迁移为 `both`；无 `RUN_MODE` 时按 ENABLE+DASH_ONLY 推导

### 运行时
- `chip` 模式仪表盘不挂卡片，仅胶囊；`card`/`both` 卡片逻辑不变
- Loader / API 注入 `run_mode`；`shouldShowCard` 增加 `wantCardUi()`

---

## v1.0.0-Beta7 — 2026-07-30 (`2026.07.30g`)

### 跨页续播 / 移动端
- 全页导航后卡片 DOM 与内存引用脱节时自动重建并重绑进度/播放控件
- 进度条直接绘制到当前文档中的 live 节点，避免卡在 `0:00`
- FLAC/SMB 中段 seek 失败导致「假播放」时自动硬重置恢复
- 仪表盘 tile 被 Unraid 替换时的卡片重建与进度同步更稳

---

## v1.0.0-Beta6 — 2026-07-30 (`2026.07.30f`)

### 仪表盘卡片布局
- 左右栏**固定宽度**（左约 248px / 右约 300px），卡片 `max-content` 不再被仪表盘列拉长
- 曲目/歌词复用区**加宽**并向左靠拢；栏间距由 14px 收至 8px
- 长标题跑马灯仍在右侧栏内裁剪，不改变整卡宽度

---

## v1.0.0-Beta5 — 2026-07-30 (`2026.07.30e`)

### 设置页
- 标题 **Theme Music → 主题音乐**
- 运行开关移到标题后（仿主题特效 service switch），去掉表单内「运行」区块
- 移除「音乐」子标题与双装提示
- 控件列宽统一：全部 select + 路径框同一 `--tm-ctrl-w`，消除长短不一

### 其它
- `theme-music-save.php` 支持标题开关仅写 SERVICE

---

## v1.0.0-Beta4 — 2026-07-30 (`2026.07.30d`)

### 设置页
- select / 路径框 / 音量条统一为同一控制列宽（`--tm-ctrl-w`），消除长短不一与错位

### 全站播放
- Loader 写入独立 `window.__UCWC_MUSIC__`，并在 DOM 就绪后重申，避免 ThemeEffects 将 `music.enable=false` 覆盖
- `ucwc-music.js` 优先读 `__UCWC_MUSIC__`
- chip 样式去掉 `all: unset`，保证非仪表盘页可见

## v1.0.0-Beta3 — 2026-07-30 (`2026.07.30c`)

### 设置页布局
- 右侧 select：`width: max-content !important`，对抗 Unraid `default-base.css` 的 `width:100%`
- 路径框/音量条：`--tm-ctrl-w` 跟随邻近 select；fileTree 绝对定位在 path-field 下方（对齐 ThemeEffects）
- 取消标题下方「已保存…」提示；保存后静默刷新

### 独立版本管理
- 新增 `theme-music-update.php`（仓库 deltrivx/ThemeMusic，jobs `/tmp/theme-music-jobs`）
- 标题栏：版本号 +「检查更新」「更新日志」（与 ThemeEffects 完全独立）
- `assets/theme-music-settings.js`：OTA/全量/进度/changelog 面板

### 安装
- `install.sh` / OTA manifest 纳入 update API 与 settings JS
- 音乐卡片仍为 ThemeMusic 路径的 ucwc-music（与主题特效功能对齐）

## v1.0.0-Beta2 — 2026-07-30 (`2026.07.30b`)

### 设置页 / 应用

- Unraid `form markdown="1"`：`Label:` / `: control` → 标准 `dt`/`dd` 行（对齐 ThemeEffects 音乐区，修复整行铺满）
- 应用按钮：监听 change/input，在 BodyInlineJS 将 `input[value=应用]` 禁用且跳过 `input.lock` 时自行解锁并允许原生 POST 保存
- 新增 `assets/theme-music-settings.css`：`--tm-ctrl-w` 路径框宽度、select 样式
- 保存后 `?applied=1` 展示成功提示；`install.sh` OTA/卸载纳入 settings CSS

## v1.0.0-Beta1 — 2026-07-30 (`2026.07.30a`)

### 首发

- 独立插件 `theme.music`：从 ThemeEffects 抽取音乐组件
- 仪表盘卡片 + 全站 mini chip；本地目录音源；LRC / 封面缓存；跨页续播尽力而为
- 设置：`ThemeMusic.page`；注入：`ThemeMusic_Loader.page`（Buttons:101）
- API：`ucwc-music-api.php`；可选 AJAX：`theme-music-save.php`
- OTA：`versions/v1.0.0-Beta1` + `files.manifest`
- 与 ThemeEffects 路径/配置/缓存完全独立；文档提示勿双开音乐